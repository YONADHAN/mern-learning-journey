const Home = () => {
    return (
        <div>
            <h1>Welcome to React Components Learning</h1>
            <p>Select a topic from the sidebar.</p>
        </div>
    );
};

export default Home;
